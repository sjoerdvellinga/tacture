version: 1
applications:
  - appRoot: apps/landing
    frontend:
      phases:
        preBuild:
          commands:
            - npm ci --include=optional
        build:
          commands:
            - npm run -w apps/landing build
      artifacts:
        baseDirectory: apps/landing/.next
        files:
          - '**/*'
      cache:
        paths:
          - node_modules/**/*